# 今日は何作る？ - 自炊レシピ提案アプリ

学生の自炊向けに、安い・時短な定番レシピをTinder風のスワイプでlike/noするだけで、
好みを学習して提案してくれるPWA(スマホのブラウザで使えるアプリ)です。

## 特徴

- 🍳 **スワイプでlike/no**: ドラッグ、または画面下のボタンでレシピを評価
- 🧠 **好みを学習**: like/noの履歴からタグ(ジャンル・調理法・主材料など)の好みスコアを
  蓄積し、次に見せるレシピを重み付き抽選。一定確率で完全ランダムな提案も混ぜることで
  ワンパターン化を防止
- 💾 **サーバー不要**: すべてスマホのブラウザ内(localStorage)に保存。ログイン不要ですぐ使える
- 📱 **PWA対応**: ホーム画面に追加してアプリのように起動可能。簡易オフラインキャッシュ対応
- ❤️ **お気に入り一覧**: likeしたレシピを後から見返せる
- 🧺 **おうちの在庫(任意機能)**: 調味料・野菜・肉魚などの在庫をチェックしておくと、
  「今の在庫で作れる」バッジが表示され、提案にも軽く反映される。お気に入りに足りない
  食材は買い物リストとして自動でまとまる
- 🤖 **AIにレシピを考えてもらう(任意機能)**: ボタンを押すと、Claude APIが好みのタグや
  在庫の食材を踏まえて新しいレシピをその場で1件生成し、通常のレシピと同じようにスワイプ
  できる。API利用料がかかるため、ユーザーが押したときだけ呼び出す設計にしている

## 技術構成

- [Next.js](https://nextjs.org)(App Router) + TypeScript
- Tailwind CSS
- [Framer Motion](https://www.framer.com/motion/)(スワイプのドラッグ操作)
- データ保存はブラウザの`localStorage`のみ(バックエンド/DBなし)

## セットアップ

```bash
npm install
npm run dev
```

[http://localhost:3000](http://localhost:3000) をスマホまたはPCブラウザで開いてください。

スマホでは、ブラウザの「ホーム画面に追加」からアプリのように追加できます(PWA)。

### AIレシピ生成機能を使う場合(任意)

「🤖 AIにレシピを考えてもらう」ボタンを使うには、[Anthropicのコンソール](https://console.anthropic.com/)
でAPIキーを発行し、環境変数 `ANTHROPIC_API_KEY` に設定してください。

- ローカル開発: プロジェクト直下に `.env.local` を作成し `ANTHROPIC_API_KEY=sk-ant-...` を記載
  (`.env*` は`.gitignore`済みなのでコミットされません)
- Vercelにデプロイする場合: プロジェクトの Settings → Environment Variables に
  `ANTHROPIC_API_KEY` を追加してから再デプロイ

APIキーを設定しなくても、この機能が使えないだけでアプリの他の機能(スワイプ・在庫・お気に入り)
は通常通り動作します。API呼び出しはボタンを押したときだけ発生し、1日10回までのローカル制限
(`src/app/page.tsx`)を設けているので、意図しない高額請求は起きにくい設計です。

## ディレクトリ構成

```
src/
  app/
    page.tsx        # メインのスワイプ画面
    liked/page.tsx  # お気に入り一覧・買い物リスト
    pantry/page.tsx # おうちの在庫チェックリスト
    layout.tsx       # 共通レイアウト・PWAメタデータ
  components/
    SwipeCard.tsx           # スワイプ可能なレシピカード
    RecipeDetailModal.tsx   # 材料・作り方の詳細モーダル
    BottomNav.tsx            # 下部タブナビゲーション
    ServiceWorkerRegister.tsx
  data/
    recipes.ts        # レシピデータ(タグ・在庫ID付き、約36件)
    pantryItems.ts      # 在庫アイテムのマスターデータ
  lib/
    storage.ts         # localStorageの読み書き(好み・在庫・AI生成レシピ)
    recommend.ts        # タグベースの推薦ロジック
    pantryMatch.ts        # レシピと在庫のマッチング計算
    aiRecipe.ts            # AIレシピ生成APIを呼び出すクライアント関数
  app/api/generate-recipe/route.ts  # Claude APIを呼び出すサーバー側エンドポイント
public/
  manifest.json        # PWAマニフェスト
  sw.js                 # 簡易Service Worker(オフラインキャッシュ)
```

## 推薦ロジックの仕組み

1. レシピにはあらかじめ「和食/中華」「炒め物/丼/麺」「鶏肉/豚肉/野菜多め」「時短/節約」
   などのタグが付いている(`src/data/recipes.ts`)
2. likeすると該当タグのスコアを+1、noすると-1(タグごとに-5〜+5でクランプ)
3. 次のレシピを選ぶとき、各レシピの保有タグの平均スコアに応じた重み付き抽選を行う
4. 25%の確率で完全ランダムに選ぶ(exploration)ことで、好みの偏りだけに支配されず
   新しいジャンルにも出会えるようにしている(ワンパターン化防止)
5. 一度見せたレシピは同じ周回中は再表示しない。全件見せ終わったら履歴をリセットして
   再度出現できるようにする

ロジック本体は `src/lib/recommend.ts` を参照してください。

## おうちの在庫機能

- `src/data/pantryItems.ts` に調味料・主食・野菜・肉魚などのマスターデータ(約57品目)を定義
- 各レシピ(`src/data/recipes.ts`)は必要な在庫アイテムIDを`pantryIds`として持つ
- 「在庫」タブでチェックした食材はlocalStorageに保存される(調味料・米・卵など常備しがち
  な品目は初期状態でチェック済み)
- `src/lib/pantryMatch.ts`の`getPantryMatch()`で、レシピごとの在庫充足率と不足食材を計算
- 推薦ロジック(`pickNextRecipe`)では在庫充足率を軽いブースト(重みを最大+30%程度)として
  使うのみで、好み学習を上書きする絶対条件にはしていない
- お気に入り一覧では、liked済みレシピ全体で不足している食材を自動集計して買い物リスト表示
- 在庫は完全に任意機能。何もチェックしなくてもアプリは通常通り動作する

## AIレシピ生成の仕組み(LLMハイブリッド)

- 普段の提案はタグベース学習(無料・高速)で行い、ユーザーがボタンを押したときだけ
  Claude API(`claude-opus-5`)を呼び出して新規レシピを1件生成する、コスト意識したハイブリッド構成
- APIキーはサーバー側の環境変数としてのみ保持し、`src/app/api/generate-recipe/route.ts`
  (Next.jsのRoute Handler)経由でのみ呼び出す。ブラウザにAPIキーを渡すことは一切しない
- 生成時は好みタグ上位5件・在庫にある食材名・直近のレシピ名(重複回避用)をプロンプトに含め、
  `output_config.format`(構造化出力)でレシピの型に沿ったJSONのみを返させている
- pantryIdsは在庫アイテムの実在するIDのみを列挙型(enum)で許可し、不正な値が入らないようにしている
- 生成されたレシピは`customRecipes`としてlocalStorageに保存され、以後は内蔵レシピと同じ
  プールでスワイプ・お気に入り・買い物リストの対象になる
- 呼び出しは明示的なボタン操作のみ(自動実行なし)。さらに1日10回までのローカル制限を設けて
  意図しない多重課金を防いでいる

## 今後の拡張アイデア

- **クラウド同期**: Supabase等を導入し、機種変更してもデータを引き継げるようにする
- **画像**: 現在はレシピごとに絵文字を表示しているだけなので、実際の調理写真を追加すると
  より魅力が伝わる
- **在庫の自動消費**: レシピを「作った」記録と連動して、使った食材を自動でチェックOFFに
  する(現状は手動チェックのみで、時間が経つと実態とズレていく可能性がある)
